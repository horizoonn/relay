type: object
additionalProperties: false
required:
  - items
properties:
  items:
    type: array
    maxItems: 100
    items:
      $ref: ./item_summary.yaml
  next_cursor:
    type: string
    minLength: 1
    maxLength: 2048
    description: Opaque continuation cursor. Omitted when there is no next page.
